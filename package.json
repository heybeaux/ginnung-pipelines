{
  "name": "@heybeaux/ginnung-pipelines-root",
  "version": "0.0.0",
  "private": true,
  "description": "Ginnung cognitive runtime dogfood pipelines (writing pipeline first).",
  "packageManager": "pnpm@10.4.0",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "lint": "echo 'lint: no-op for now'"
  },
  "license": "UNLICENSED"
}
