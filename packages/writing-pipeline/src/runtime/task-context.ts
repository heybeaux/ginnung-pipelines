// Per-task context carried across steps — task_id, slug, budgets, fingerprint
// pointer, run mode (production vs stress-test). Stub.

// TODO: implement.
export function createTaskContext(): never {
  throw new Error('not implemented');
}
