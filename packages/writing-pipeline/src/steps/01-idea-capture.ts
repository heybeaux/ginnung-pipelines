// Step 1: idea capture — a one-line idea hits an inbox (reminders / voice / chat).
// Emits SonderEvent { step: 'idea-capture' }. Stub.

// TODO: implement.
export function ideaCapture(): never {
  throw new Error('not implemented');
}
